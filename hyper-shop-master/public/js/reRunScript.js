for (let btn of document.getElementsByClassName("addToCartBtn")) {
  btn.addEventListener("click", addToCartHandler);
}
for (let btn of document.getElementById("chooseSizeOpt")) {
  btn.addEventListener("click", addToCartHandler);
}
